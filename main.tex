\documentclass[12pt]{article}

\usepackage{amsmath}
\usepackage{graphicx}
\usepackage[
  backend=biber,
  style=authoryear
]{biblatex}



\addbibresource{refs.bib}

\title{
A Mechanistic Model for Radiation-Induced Segregation Controlled
Grain-Boundary Oxidation in Austenitic Stainless Steels
}

\author{Zhuo Wang \and Md Shahrier Hasan \and Jaime Marian}
\date{\today}

% \setlength{\parindent}{0pt}
% \setlength{\parskip}{0.8em}

\newcommand{\Vac}{\mathrm{V}}          % vacancy
\newcommand{\Int}{\mathrm{I}}          % self-interstitial
\newcommand{\GB}{\mathrm{GB}}
\newcommand{\eff}{\mathrm{eff}}
\newcommand{\intf}{\mathrm{int}}       % interface
\newcommand{\thr}{\mathrm{th}}         % threshold
\newcommand{\lat}{\mathrm{lat}}
\newcommand{\ox}{\mathrm{ox}}
\newcommand{\Ox}{\mathrm{O}}           % oxygen

\usepackage{graphicx}
\usepackage{subcaption}
\graphicspath{{figures/}}

\usepackage{siunitx}
\usepackage{mhchem}

\DeclareSIUnit{\dpa}{dpa}
\DeclareSIUnit{\atpct}{at.\%}



\begin{document}

\maketitle


% ============================================================
\section{Introduction}
\label{sec:introduction}


% ============================================================
\section{Model Formulation and Calibration}
\label{sec:method}

\begin{figure}[htbp]\centering
% the panels are drawn at ~9 cm each; the block is set wider than the text
% column so that they print close to 1:1 and their labels stay legible
\makebox[\textwidth][c]{%
\begin{minipage}{18.6cm}\centering
\begin{subfigure}[t]{0.49\linewidth}\centering
  \includegraphics[width=\linewidth]{framework_a}
  \caption{}\label{fig:framework-domain}
\end{subfigure}\hfill
\begin{subfigure}[t]{0.49\linewidth}\centering
  \includegraphics[width=\linewidth]{framework_b}
  \caption{}\label{fig:framework-ris}
\end{subfigure}

\vspace{1em}

\begin{subfigure}[t]{0.49\linewidth}\centering
  \includegraphics[width=\linewidth]{framework_c}
  \caption{}\label{fig:framework-ox}
\end{subfigure}\hfill
\begin{subfigure}[t]{0.49\linewidth}\centering
  \includegraphics[width=\linewidth]{framework_d}
  \caption{}\label{fig:framework-coupling}
\end{subfigure}
\end{minipage}}
\caption{Overview of the model: (a) physical picture and computational domain,
(b) radiation-induced segregation, (c) oxygen transport across the oxide and the
interfacial reactions, and (d) coupling between the two components.}
\label{fig:framework}
\end{figure}


The model consists of two components: a radiation-induced segregation (RIS) model for the evolving GB chemistry and a reduced-dimensional oxidation model for oxygen transport, consumption, and oxide growth. The RIS-predicted Cr, Ni, Si, and Fe concentrations provide the local alloy composition for the oxidation model. Oxygen transport along the GB is solved explicitly, while transport through the oxide and interfacial reactions are treated using a quasi-steady resistance formulation. The oxide composition determines the effective oxygen diffusivity through an effective-medium treatment, which controls the local oxygen consumption and oxide growth rate. The formulations of the two components are described below.

Figure~\ref{fig:framework} summarises the model. The computational domain,
Fig.~\ref{fig:framework-domain}, is a grain boundary of width \(\delta_{\GB}\)
that opens onto the surface exposed to the primary water. Oxygen enters at the
mouth through the surface film and migrates along the boundary. While the oxide
is thinner than \(\delta_{\GB}\) the boundary retains free volume, which carries
a fast oxygen path; once the oxide fills the channel that contribution is lost.
Only the half-domain on one side of the boundary is simulated, the other half
following by symmetry.

The boundary acts as a perfect sink for the vacancies and self-interstitials
produced by irradiation, Fig.~\ref{fig:framework-ris}. The resulting
redistribution depletes Cr and enriches Ni and Si at the boundary, and the
imbalance between the two defect fluxes gives the segregation contribution
\(\mathbf{v}_{\lat}^{\mathrm{RIS}}=\mathbf{J}_{\Vac}-\mathbf{J}_{\Int}\) to the
lattice velocity; the profiles shown are calculated, mirrored about the boundary
and convolved with a \SI{1}{\nano\metre} FWHM Gaussian. At a given depth,
Fig.~\ref{fig:framework-ox}, the oxygen delivered along the boundary crosses the
oxide in quasi-steady state and is partitioned at the fixed oxide/metal interface
among the four oxide phases; the metal removed there sets the oxidation
contribution \(v_{\ox}\). The two components are coupled as shown in
Fig.~\ref{fig:framework-coupling}: irradiation fixes the boundary chemistry and
part of the lattice velocity, the boundary chemistry controls the interfacial
reaction rates, and the oxide that forms feeds back on the oxygen supply through
its thickness and its effective diffusivity.

\paragraph{Notation.}
Alloying-element indices are \(i,j\in\{\mathrm{Cr},\mathrm{Ni},\mathrm{Si},\mathrm{Fe}\}\);
oxide-phase indices are
\(k\in\{\mathrm{Cr_2O_3},\mathrm{Fe_3O_4},\mathrm{FeCr_2O_4},\mathrm{SiO_2}\}\).
Superscripts \(\Vac\) and \(\Int\) denote quantities associated with vacancies and
self-interstitials; the superscript \(\GB\) denotes a value taken on the grain
boundary. \(C\) is a site fraction, \(\mathbf{J}\) a flux in the two-dimensional metal
domain, and \(J\) a flux on the one-dimensional grain-boundary line. The coordinate
\(x\) is normal to the boundary and \(y\) runs along it.

\subsection{Radiation-Induced Segregation Model}
\label{sec:methodRIS}

The RIS model is based on solving a set of coupled mass-conservation equations for the
alloying elements (Cr, Ni, Si, and Fe) and point defects (vacancies and
self-interstitials). For each alloying element \(i\), the concentration evolves
according to
\begin{equation}
\frac{\partial C_i}{\partial t}=-\nabla\!\cdot\mathbf{J}_i .
\label{eq:mass-metal}
\end{equation}
The site fractions of vacancies and self-interstitials are governed separately by
\begin{align}
\frac{\partial C_{\Vac}}{\partial t}
 &= -\nabla\!\cdot\mathbf{J}_{\Vac}+\epsilon K_0-R\,C_{\Int}C_{\Vac},
\label{eq:mass-vac}\\
\frac{\partial C_{\Int}}{\partial t}
 &= -\nabla\!\cdot\mathbf{J}_{\Int}+\epsilon K_0-R\,C_{\Int}C_{\Vac},
\label{eq:mass-int}
\end{align}
where \(C_{\Vac}\) and \(C_{\Int}\) are the vacancy and self-interstitial site
fractions, \(\epsilon\) is the damage efficiency, \(K_0\) is the radiation dose rate,
and \(R\) is the vacancy--interstitial recombination rate coefficient. The flux of each
alloying element carried by each defect mechanism is
\begin{align}
\mathbf{J}_i^{\Vac} &= -\sum_j L_{ij}^{\Vac}\,
   \nabla\!\left(\mu_j-\mu_{\Vac}\right),
\label{eq:flux-vac}\\
\mathbf{J}_i^{\Int} &= -\sum_j L_{ij}^{\Int}\,
   \nabla\!\left(\mu_j+\mu_{\Int}\right),
\label{eq:flux-int}
\end{align}
with the Onsager coefficients
\begin{align}
L_{ii}^{\Vac} &= \frac{C_iD_i^{\Vac}C_{\Vac}}{k_BT}
  \left(1+\frac{1-f_0^{\Vac}}{f_0^{\Vac}}\,
  \frac{C_iD_i^{\Vac}}{\sum_m C_mD_m^{\Vac}}\right),
\label{eq:onsager-vac-diag}\\
L_{ij}^{\Vac} &= L_{ji}^{\Vac}
  = \frac{1-f_0^{\Vac}}{f_0^{\Vac}}\,
    \frac{C_iD_i^{\Vac}\,C_jD_j^{\Vac}\,C_{\Vac}}
         {k_BT\,\sum_m C_mD_m^{\Vac}},
    \qquad i\neq j,
\label{eq:onsager-vac-off}\\
L_{ii}^{\Int} &= \frac{C_iD_i^{\Int}C_{\Int}}{k_BT}
  \left(1+\frac{1-f_0^{\Int}}{f_0^{\Int}}\,
  \frac{C_iD_i^{\Int}}{\sum_m C_mD_m^{\Int}}\right),
\label{eq:onsager-int-diag}\\
L_{ij}^{\Int} &= L_{ji}^{\Int}
  = \frac{1-f_0^{\Int}}{f_0^{\Int}}\,
    \frac{C_iD_i^{\Int}\,C_jD_j^{\Int}\,C_{\Int}}
         {k_BT\,\sum_m C_mD_m^{\Int}},
    \qquad i\neq j,
\label{eq:onsager-int-off}
\end{align}
where \(D_i^{\Vac}\) and \(D_i^{\Int}\) are the mechanism-specific diffusion
coefficients of element \(i\), and \(f_0^{\Vac}\) and \(f_0^{\Int}\) are the
corresponding correlation factors. The net defect fluxes follow from conservation of
lattice sites,
\begin{align}
\mathbf{J}_{\Vac} &= -\sum_i \mathbf{J}_i^{\Vac},
\label{eq:netflux-vac}\\
\mathbf{J}_{\Int} &= \sum_i \mathbf{J}_i^{\Int},
\label{eq:netflux-int}
\end{align}
and give rise to a lattice velocity
\begin{equation}
\mathbf{v}_{\lat}^{\mathrm{RIS}}=\mathbf{J}_{\Vac}-\mathbf{J}_{\Int} .
\label{eq:vlat-ris}
\end{equation}

\subsection{Grain-Boundary Oxidation Model}
\label{sec:methodGBOxidation}

Oxygen transport along the boundary, treated as a slab of width \(\delta_{\GB}\),
obeys
\begin{align}
\frac{\partial C_{\Ox}}{\partial t}
 &= -\frac{\partial J_{\Ox}}{\partial y}
    -\frac{J_{\mathrm{r}}}{\delta_{\GB}},
\label{eq:O-mass}\\
J_{\Ox} &= -D_{\Ox}^{\eff}\,\frac{\partial C_{\Ox}}{\partial y},
\label{eq:O-flux}
\end{align}
where the effective diffusivity is obtained from an effective-medium mixture of the
oxide phases,
\begin{equation}
D_{\Ox}^{\eff}=\sum_k f_k\,D_{\Ox}^{k},
\label{eq:Deff}
\end{equation}
with \(f_k\) the volume fraction and \(D_{\Ox}^{k}\) the oxygen diffusivity of phase
\(k\). Oxygen delivery from the boundary channel to the metal/oxide interface is
treated as a quasi-steady series resistance across the oxide of total thickness
\(\sum_k L_k\),
\begin{equation}
J_{\mathrm{r}}
 = D_{\Ox}^{\eff}\,
   \frac{C_{\Ox}-C_{\Ox}^{\intf}}{\sum_k L_k},
\label{eq:film-transport}
\end{equation}
where \(L_k\) is the thickness of oxide \(k\) and \(C_{\Ox}^{\intf}\) is the oxygen
concentration at the interface. The interfacial reaction rates of the four oxide phases
are
\begin{align}
J_{\mathrm{r}}^{\mathrm{Cr_2O_3}}
 &= k_{\mathrm{r}}^{\mathrm{Cr_2O_3}}\,
    C_{\Ox}^{\intf}\,C_{\mathrm{Cr}}^{\GB},
\label{eq:rate-cr2o3}\\
J_{\mathrm{r}}^{\mathrm{Fe_3O_4}}
 &= k_{\mathrm{r}}^{\mathrm{Fe_3O_4}}
    \left(C_{\Ox}^{\intf}-C_{\Ox}^{\thr}\right)
    C_{\mathrm{Fe}}^{\GB},
\label{eq:rate-fe3o4}\\
J_{\mathrm{r}}^{\mathrm{FeCr_2O_4}}
 &= k_{\mathrm{r}}^{\mathrm{FeCr_2O_4}}\,
    C_{\Ox}^{\intf}
    \left(\frac{1}{C_{\mathrm{Fe}}^{\GB}}
         +\frac{2}{C_{\mathrm{Cr}}^{\GB}}\right)^{-1},
\label{eq:rate-fecr2o4}\\
J_{\mathrm{r}}^{\mathrm{SiO_2}}
 &= k_{\mathrm{r}}^{\mathrm{SiO_2}}\,
    C_{\Ox}^{\intf}\,C_{\mathrm{Si}}^{\GB},
\label{eq:rate-sio2}
\end{align}
where \(k_{\mathrm{r}}^{k}\) is the interfacial rate constant of phase \(k\) and
\(C_{\Ox}^{\thr}\) is the threshold oxygen concentration for magnetite formation.
Their sum gives the total interfacial consumption,
\begin{equation}
J_{\mathrm{r}}
 = J_{\mathrm{r}}^{\mathrm{Cr_2O_3}}+J_{\mathrm{r}}^{\mathrm{Fe_3O_4}}
  +J_{\mathrm{r}}^{\mathrm{FeCr_2O_4}}+J_{\mathrm{r}}^{\mathrm{SiO_2}} .
\label{eq:rate-total}
\end{equation}
Equations~\eqref{eq:film-transport} and \eqref{eq:rate-total} constitute the
quasi-steady balance between oxygen supply and oxygen consumption,
\begin{equation}
D_{\Ox}^{\eff}\,\frac{C_{\Ox}-C_{\Ox}^{\intf}}{\sum_k L_k}
=\sum_k J_{\mathrm{r}}^{k}\!\left(C_{\Ox}^{\intf}\right),
\label{eq:qss-closure}
\end{equation}
which is solved at every position for the single unknown \(C_{\Ox}^{\intf}\); the
supply term decreases and the consumption term increases monotonically with
\(C_{\Ox}^{\intf}\), so the root is unique. The corresponding rates of metal removal
follow from the stoichiometry of each phase,
\begin{align}
J_{\mathrm{r}}^{\mathrm{Cr}}
 &= \tfrac{2}{3}J_{\mathrm{r}}^{\mathrm{Cr_2O_3}}
   +\tfrac{2}{4}J_{\mathrm{r}}^{\mathrm{FeCr_2O_4}},
\label{eq:sink-cr}\\
J_{\mathrm{r}}^{\mathrm{Fe}}
 &= \tfrac{3}{4}J_{\mathrm{r}}^{\mathrm{Fe_3O_4}}
   +\tfrac{1}{4}J_{\mathrm{r}}^{\mathrm{FeCr_2O_4}},
\label{eq:sink-fe}\\
J_{\mathrm{r}}^{\mathrm{Si}}
 &= \tfrac{1}{2}J_{\mathrm{r}}^{\mathrm{SiO_2}} .
\label{eq:sink-si}
\end{align}

\subsection{Coupling Strategy and Simulation Protocol}
\label{sec:methodCoupling}

\subsubsection{Lattice Velocity and Metal Consumption}
\label{sec:methodLattice}
The interfacial reactions remove metal atoms only at the grain-boundary plane.
Because the metal lattice is incompressible and the boundary is a perfect defect
sink, this removal creates no source in the interior: the entire lattice
translates towards the interface at the velocity set by the interfacial
consumption. Denoting by
\begin{equation}
v_{\ox}(y,t)=J_{\mathrm{r}}^{\mathrm{Cr}}+J_{\mathrm{r}}^{\mathrm{Fe}}
  +J_{\mathrm{r}}^{\mathrm{Si}}
\label{eq:vlat-ox}
\end{equation}
the recession rate of the metal/oxide interface, the total lattice velocity in
the metal domain is
\begin{equation}
\mathbf{v}_{\lat}(x,y,t)
 =\mathbf{v}_{\lat}^{\mathrm{RIS}}(x,y,t)
  +v_{\ox}(y,t)\,\hat{\mathbf{e}}_{x} .
\label{eq:vlat-total}
\end{equation}
The two contributions differ in character. The segregation term is local: it is
determined pointwise by the defect fluxes at \((x,y)\). The oxidation term is
not; it is evaluated at the boundary and is independent of \(x\), so a single
value applies across the whole domain at a given \(y\). Segregation therefore
redistributes the lattice internally, whereas oxidation translates it rigidly.so that the total flux of alloying element \(i\) entering
Eq.~\eqref{eq:mass-metal} is
\begin{equation}
\mathbf{J}_i
 = \mathbf{J}_i^{\Vac}+\mathbf{J}_i^{\Int}
  +C_i\mathbf{v}_{\lat} ,
\label{eq:flux-total}
\end{equation}
subject to the interfacial condition
\begin{equation}
\left.\mathbf{J}_i\!\cdot\!\hat{\mathbf{e}}_{x}\right|_{x=0}
 = J_{\mathrm{r}}^{i} ,
\label{eq:flux-bc}
\end{equation}
which removes from the boundary plane the atoms incorporated into the growing
oxide.

Equations~\eqref{eq:flux-total} and \eqref{eq:flux-bc} collect the three ways in which
an alloying element can move. The first two terms are the diffusive fluxes carried by
the two defect mechanisms, Eqs.~\eqref{eq:flux-vac} and \eqref{eq:flux-int}; the third
is the drift of the entire lattice relative to the laboratory frame; the interfacial
consumption enters not as a bulk term but as an outgoing flux on the boundary plane.

The two contributions to \(\mathbf{v}_{\lat}\) have different origins but act
identically on every species. \(\mathbf{v}_{\lat}^{\mathrm{RIS}}\) arises because the
vacancy and interstitial fluxes are unequal, so lattice sites are created and destroyed
at different rates; \(v_{\ox}\) arises because oxidation consumes metal atoms from the
interfacial layer, so the lattice is displaced towards the advancing front. Because the
drift term \(C_i\mathbf{v}_{\lat}\) is proportional to the local concentration and
carries no species selectivity, it does not by itself alter the relative composition;
selectivity enters only through the mechanism-specific diffusivities in
Eqs.~\eqref{eq:onsager-vac-diag}--\eqref{eq:onsager-int-off} and through the
stoichiometric sinks in Eqs.~\eqref{eq:sink-cr}--\eqref{eq:sink-si}.

\subsubsection{Sequential Irradiation--Oxidation Protocol}
\label{sec:methodSequential}



The reference experiments irradiate the specimen first and expose it to the primary
water environment afterwards. The model reproduces this protocol as two consecutive
stages that share the same governing equations and differ only in two switches: the
dose rate \(K_0\) and the oxygen boundary condition at the grain-boundary mouth.

In the irradiation stage the specimen is irradiated to the target dose with no oxygen
present, \(K_0>0\) and \(C_{\Ox}\equiv 0\), so that
Eqs.~\eqref{eq:mass-metal}--\eqref{eq:vlat-ris} govern the evolution alone and all
interfacial reaction rates vanish identically. The stage is integrated to
\(t_{\mathrm{irr}}=\Phi/K_0\), where \(\Phi\) is the nominal dose in dpa, and yields the
segregated composition profiles \(C_i(x,y,t_{\mathrm{irr}})\).

The oxidation stage continues from the final state of the irradiation stage: all
concentration fields, including the point-defect populations, are inherited unchanged,
\begin{equation}
\left.\left(C_i,\,C_{\Vac},\,C_{\Int}\right)\right|_{t=0^{+}}
=\left.\left(C_i,\,C_{\Vac},\,C_{\Int}\right)\right|_{t=t_{\mathrm{irr}}},
\qquad
\left.L_k\right|_{t=0^{+}}=0 ,
\label{eq:handoff}
\end{equation}
and the exposure clock is restarted from zero. The dose rate is then set to zero,
\(K_0=0\), so that the inherited defect supersaturation decays by recombination
according to Eqs.~\eqref{eq:mass-vac} and \eqref{eq:mass-int} without further
production.

Simultaneously, the oxygen boundary condition at the mouth of the boundary is switched
on. Oxygen ingress is limited by the surface film that grows on the free surface, whose
resistance increases as the film thickens; this is imposed as a Robin condition with a
time-decaying transfer coefficient,
\begin{equation}
\left.J_{\Ox}\right|_{y=0}
= h(t)\left(C_{\Ox}^{\mathrm{w}}-\left.C_{\Ox}\right|_{y=0}\right),
\qquad
h(t)=\frac{k_{\mathrm{s}}}{\sqrt{t}+\sqrt{t_0}} ,
\label{eq:robin}
\end{equation}
where \(C_{\Ox}^{\mathrm{w}}\) is the oxygen concentration of the bulk water,
\(k_{\mathrm{s}}\) is the surface transfer coefficient, and \(t_0\) regularizes the
initial instant. The \(t^{-1/2}\) decay reproduces the parabolic thickening of the
surface film, so that the cumulative oxygen admitted through the mouth grows as
\(t^{1/2}\). The deep end of the boundary is closed,
\(\left.J_{\Ox}\right|_{y=L_{\GB}}=0\).

Because no point defects are produced once \(K_0=0\), the inherited supersaturation
decays on the recombination timescale, which is short compared with the exposure time;
segregation therefore effectively ceases early in the exposure stage, and the
composition field that the oxidation model sees is the one established during
irradiation. The sequential protocol thus isolates the compositional pathway: any dose
dependence of the predicted oxidation depth must be carried by the radiation-induced
segregation profile inherited through Eq.~\eqref{eq:handoff}, since neither radiolysis
nor sustained irradiation-assisted transport is present.

\subsubsection{Fully Coupled Irradiation--Oxidation Framework}
\label{sec:methodFullyCoupled}

The sequential protocol of Sec.~\ref{sec:methodSequential} is a special case of the
governing equations rather than a separate model: it is obtained by switching \(K_0\)
and the mouth boundary condition on and off in turn. Leaving both switches on
throughout, \(K_0>0\) together with the Robin condition Eq.~\eqref{eq:robin}, gives the
fully coupled problem in which segregation and oxidation proceed simultaneously, as
they do in reactor service. No additional equations or parameters are required;
Eqs.~\eqref{eq:mass-metal}--\eqref{eq:vlat-ris} are integrated as a single stage.

In this mode the two processes feed back on each other. Segregation continues to
modify the boundary composition while the oxidation front advances, so the composition
that the interfacial reactions see evolves in time rather than being fixed at its
end-of-irradiation value; conversely, the metal consumed at the front and the
associated lattice drift, Eq.~\eqref{eq:vlat-ox}, alter the concentration field on
which the segregation fluxes act. Predictions obtained in this mode are reported in
Sec.~\ref{sec:resultsFullyCoupled}; because no experiment in the present dataset
imposes concurrent irradiation and exposure, this mode is used only for prediction and
takes no part in the calibration of Sec.~\ref{sec:methodCalibration}.

\subsection{Model Parameterization and Calibration}
\label{sec:methodCalibration}
The two components are parameterized and calibrated separately and against separate data. The RIS model is calibrated first; the oxidation model is then calibrated with the RIS model related parameters fixed. 

\subsubsection{RIS Parameterization}
\label{sec:methodRISCalibration}

The RIS model requires the mechanism-specific diffusion coefficients
\(D_i^{\Vac}\) and \(D_i^{\Int}\) of the four alloying elements, the correlation
factors \(f_0^{\Vac}\) and \(f_0^{\Int}\), the recombination coefficient \(R\),
the damage efficiency \(\epsilon\), and the dose rate \(K_0\). The dose rate and
irradiation temperature are those of the reference experiment. The correlation
factors are taken from their crystallographic values for the fcc lattice. The
damage efficiency accounts for the fraction of displacements that survive
in-cascade recombination and escape as freely migrating defects; its value is
that reported for proton irradiation. The vacancy and interstitial migration
energies and pre-exponential factors are taken from the literature and adjusted
within their reported ranges to reproduce the measured grain-boundary
composition profiles across the dose series. Because these profiles are measured
before exposure, the RIS calibration is independent of the oxidation model. The
resulting values are given in Table~\ref{tab:params-ris}.


\subsubsection{Oxidation Kinetics Parameterization}
\label{sec:methodGBOXidationCalibration}
The oxidation model requires the interfacial rate constants \(k_{\mathrm{r}}^{k}\)
of the four oxide phases, their oxygen diffusivities \(D_{\Ox}^{k}\), the surface
transfer coefficient \(k_{\mathrm{s}}\) of the Robin condition
Eq.~\eqref{eq:robin}, and the magnetite threshold \(C_{\Ox}^{\thr}\). The oxide
molar volumes and densities used in the thickness update are taken from
crystallographic data and held fixed.

The model is calibrated against the dose-dependent grain-boundary oxidation
depths of, measured after exposure in simulated PWR primary water
at 0, 0.5, and 3 dpa. The predicted oxidation depth is defined as the
distance from the boundary mouth to the deepest position at which the total oxide
thickness \(\sum_k L_k\) exceeds a fixed threshold \(L_{\thr}\), applied
identically at every dose. The free parameters are adjusted to reproduce the
measured depths at all three doses simultaneously, using a single parameter set;
no parameter is allowed to take different values at different doses. The
resulting values are given in Table~\ref{tab:params-ox}.

\begin{table}[htbp]
\centering
\caption{Parameters of the radiation-induced segregation model.}
\label{tab:params-ris}
\begin{tabular}{llll}
\hline
Symbol & Description & Value & Source \\
\hline
 & & & \\
\hline
\end{tabular}
\end{table}

\begin{table}[htbp]
\centering
\caption{Parameters of the grain-boundary oxidation model.}
\label{tab:params-ox}
\begin{tabular}{llll}
\hline
Symbol & Description & Value & Source \\
\hline
 & & & \\
\hline
\end{tabular}
\end{table}



% ============================================================
\section{Results}
\label{sec:results}

\subsection{Radiation-Induced Segregation Profiles}
\label{sec:resultsRIS}
\begin{figure}[htbp]\centering
\begin{subfigure}[t]{0.32\textwidth}\centering
  \includegraphics[width=\linewidth]{profile_cr}
  \caption{}\label{fig:profile-cr}
\end{subfigure}\hfill
\begin{subfigure}[t]{0.32\textwidth}\centering
  \includegraphics[width=\linewidth]{profile_ni}
  \caption{}\label{fig:profile-ni}
\end{subfigure}\hfill
\begin{subfigure}[t]{0.32\textwidth}\centering
  \includegraphics[width=\linewidth]{profile_si}
  \caption{}\label{fig:profile-si}
\end{subfigure}
\caption{Calculated and measured grain-boundary composition profiles after
irradiation to 0.5 and \SI{3.0}{dpa}: (a) Cr, (b) Ni, (c) Si. Solid lines are
calculated profiles convolved with a \SI{1}{nm} Gaussian; open and filled symbols
are the measurements of at 0.5 and \SI{3.0}{dpa} respectively.}
\label{fig:profiles}
\end{figure}

Figure~\ref{fig:profiles} compares the calculated and measured grain-boundary composition profiles after irradiation to 0.5 and \SI{3.0}{dpa}. The calculated profiles are convolved with a \SI{1}{nm} Gaussian to account for the finite spatial resolution of the measurements.  These profiles are the target of the RIS calibration of Sec.~\ref{sec:methodRISCalibration}.

Cr is depleted at the boundary and Ni and Si are enriched, at both doses. The boundary Cr concentration falls from a matrix value of \SI{20.4}{\atpct} to \SI{14.9}{\atpct} at \SI{0.5}{\dpa} and to \SI{11.8}{\atpct} at \SI{3.0}{\dpa}, against measured values of \SI{16.2}{\atpct} and \SI{11.8}{\atpct}
(Fig.~\ref{fig:profile-cr}). Ni rises from \SI{8.9}{\atpct} to \SI{11.1}{\atpct} and \SI{14.1}{\atpct} against measured \SI{12.1}{\atpct} and \SI{13.8}{\atpct}
(Fig.~\ref{fig:profile-ni}). Si rises from \SI{0.27}{\atpct} to
\SI{1.1}{\atpct} and \SI{2.4}{\atpct} against measured \SI{1.5}{\atpct} and \SI{2.7}{\atpct} (Fig.~\ref{fig:profile-si}).

Overall, the RIS profiles show clear dose-dependent changes in both the magnitude and spatial distribution of grain-boundary segregation. The most pronounced changes are observed in the grain-boundary enrichment or depletion levels, while the characteristic widths of the segregation profiles remain broadly similar across doses. Only modest differences in the full width at half maximum (FWHM) are observed, generally on the order of 1-\SI{2}{nm}, with the most noticeable changes occurring for Cr and Si at \SI{3}{dpa}. Thus, the dose dependence is primarily reflected in the segregation magnitude rather than substantial changes in the profile width.

\subsection{Oxide Composition and Interfacial Ni Enrichment}
\label{sec:resultsNiEnrichment}

\begin{figure}[!ht]\centering
\begin{subfigure}[t]{0.48\textwidth}\centering
  \includegraphics[width=\linewidth]{oxide_composition_percent}
  \caption{}\label{fig:ox-comp}
\end{subfigure}\hfill
\begin{subfigure}[t]{0.48\textwidth}\centering
  \includegraphics[width=\linewidth]{SiO2_fraction}
  \caption{}\label{fig:ox-sio2}
\end{subfigure}

\vspace{1em}

\begin{subfigure}[t]{0.48\textwidth}\centering
  \includegraphics[width=\linewidth]{Ni_at_GB}
  \caption{}\label{fig:ni-gb}
\end{subfigure}
\caption{Calculated and measured grain-boundary chemistry as a function of dose.
(a) Oxide composition on a metal (cation) basis, evaluated at the depth of the
corresponding EDX line scan: 23, 30 and \SI{44}{nm} for 0, 0.5 and
\SI{3.0}{\dpa}. (b) \ce{SiO2} volume fraction in the oxide, both at the same
depths and averaged over the oxidized length of the boundary. (c) Ni
concentration at the grain boundary before and after exposure; calculated
profiles are convolved with a Gaussian of \SI{1}{nm} FWHM to match the quoted
EDX probe diameter and are sampled at the depth of the measurement. Filled
symbols with solid lines are calculations, open symbols with dashed lines the
measurements of \textcite{deng}.}
\label{fig:gb-chemistry}
\end{figure}

Figure~\ref{fig:gb-chemistry} compares the calculated grain-boundary chemistry
with the measurements of \textcite{deng}. Because the reported EDX line scans
were taken at a single depth on each specimen, the calculated quantities are
evaluated at the corresponding depths, measured from the micrographs as 23, 30
and \SI{44}{nm} for 0, 0.5 and \SI{3.0}{\dpa}.

At those depths the model reproduces the measured dose dependence of the oxide
cation composition (Fig.~\ref{fig:ox-comp}). The calculated Cr content falls
from 84 to \SI{65}{\atpct} between 0 and \SI{3.0}{\dpa}, against a measured
decrease from 75 to \SI{58}{\atpct}; Fe rises from 15 to \SI{36}{\atpct},
against a measured 24 to \SI{38}{\atpct}. %% 核对数值
Both trends follow from the Cr depletion established during irradiation: less Cr
is available at the front, so the oxide that forms there is correspondingly
poorer in Cr and richer in Fe. Ni oxidation is not included in the model, so the
calculated Ni content of the oxide is identically zero; the measurements give a
few at.\% at all three doses, wich slighly increase with dose due to the grain boundary positive segregation.

The measured Si content of the oxide is close to zero at every dose, while the
calculated \ce{SiO2} volume fraction rises from 1.1 to \SI{25.6}{\percent} over
the same dose range (Fig.~\ref{fig:ox-sio2}). The two are not in conflict.
Silica formed at the grain boundary is not retained: the segregated Si is
oxidised preferentially and the resulting \ce{SiO2} dissolves into the water,
leaving porosity behind in the intergranular oxide \parencite{du}. Its absence
from the line scans is therefore expected even where the model predicts that a
substantial amount has formed.

The model adopts this behaviour explicitly. All \ce{SiO2} produced is treated as
dissolved, and the volume it occupied is retained as free volume available for
oxygen transport. The calculated \ce{SiO2} volume fraction is thus a measure of
the porosity generated within the intergranular oxide rather than of a phase
present in it, and it increases with dose because Si segregation to the boundary
increases with dose.




\subsection{Dose-Dependent Grain-Boundary Oxidation Kinetics}
\label{sec:resultsOxi}
\begin{figure}[!ht]\centering
\begin{subfigure}[t]{0.48\textwidth}\centering
  \includegraphics[width=\linewidth]{front_vs_dose}
  \caption{}\label{fig:front-dose}
\end{subfigure}\hfill
\begin{subfigure}[t]{0.48\textwidth}\centering
  \includegraphics[width=\linewidth]{front_vs_time}
  \caption{}\label{fig:front-time}
\end{subfigure}
\caption{Calculated and measured oxidation front depth, taken at a total oxide
thickness of \SI{1}{nm}. (a) Dependence on dose after the \SI{500}{h} exposure;
these measurements are the ones used to calibrate the oxidation parameters.
(b) Dependence on exposure time at 0.5 and \SI{1.5}{\dpa}, obtained with the
calibrated parameters held fixed; neither the \SI{1.5}{\dpa} dose nor the
exposure times shown enter the calibration. Measurements from \textcite{deng}.}
\label{fig:front}
\end{figure}



\subsection{Predictions against Independent Experiments}
\label{sec:resultsPredictiveValidation}




\subsection{Fully Coupled Predictions under Concurrent Irradiation and Oxidation}
\label{sec:resultsFullyCoupled}


% ============================================================
\section{Discussion}
\label{sec:discussion}

\subsection{Effect of Radiation-Induced Segregation on Grain-Boundary Oxidation}
\label{sec:discussionRIS}

\subsection{Rate-Controlling Transport and Oxide Chemistry}
\label{sec:discussionTransport}

\subsection{Implications of Concurrent Irradiation and Oxidation}
\label{sec:discussionConcurrent}

\subsection{Predictive Capability and Model Limitations}
\label{sec:discussionLimitations}


% ============================================================
\section{Conclusions}
\label{sec:conclusion}


% ============================================================
\printbibliography

\end{document}